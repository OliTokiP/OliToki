# Google service account keys — never commit
secrets/
*.json
!package.json
!tsconfig.json
!data/manager-fallback.json

# Local build / agent droppings (Suite App build, agent exits, etc.)
Suite.app/
grok.exit
grok-*.log

# Local / OS
.DS_Store
__pycache__/
*.pyc
.venv/
venv/

# Logs & local server noise
*.log
/tmp/
.toki-*.log

# Debug HTML droppings
_index*.html
_dbg*.html
_d*.html
_i*.html

# Full-folder zip backups (keep outside repo or untracked)
tokimenu.zip
*.zip

# Sheet snapshots next to git commits (local only; see backups/sheet-snapshots/README.md)
backups/sheet-snapshots/INDEX.txt
backups/sheet-snapshots/Menu-sheet-*.zip

# Editor / OS
*.swp
.idea/
.vscode/

# Huge design masters (over GitHub 100MB limit)
*.psd
